{
  "name": "deno",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "deno run --allow-env --allow-net --allow-read --importmap=config/import_map.json -c tsconfig.json server.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "typescript-deno-plugin": "^1.4.1"
  }
}
